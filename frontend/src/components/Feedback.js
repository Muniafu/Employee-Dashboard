import React from "react";

const Feedback = () => {
    return (
        <div className="feedback">
            <h2> Feedback </h2>
            {/* Feedback content here */}
        </div>
    );
};

export default Feedback;